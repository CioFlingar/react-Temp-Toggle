# Dependency Management
# Ignore all dependencies installed by package managers
node_modules/

# Build and Distribution Outputs
# Files generated during the build process
dist/
dist-ssr/
build/
out/
public/ # Only include this line if your BUILD OUTPUT is placed here (e.g. via Parcel or custom config)

# Logs and Temporary Files
# General logs and OS temp files
logs
*.log
*.tmp
*.bak

# Package Manager Specific Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment Variables and Secrets
# Local environment files containing secrets/API keys
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.local # Catches any file ending in .local (like .env.local)

# Editor and OS Specific Files
# VS Code
.vscode/
!.vscode/extensions.json # Don't ignore this specific file (useful for team extension recommendations)

# General IDE/OS files
.idea/
.DS_Store # macOS specific file
Thumbs.db # Windows specific file

# Swap files created by editors like Vim/Nano
*.sw?
*.swp

# Project/Solution files (common in .NET/C# environments, but harmless to keep here)
*.suo
*.ntvs*
*.njsproj
*.sln